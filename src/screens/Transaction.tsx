import {View, Text} from 'react-native';
import React from 'react';
import {styles} from '../styles/globals';

const Transaction = () => {
  return (
    <View style={styles.container}>
      <Text style={styles.contentText}>Transactions</Text>
    </View>
  );
};

export default Transaction;
